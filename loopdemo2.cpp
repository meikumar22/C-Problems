#include <stdio.h>

int main() {
    int days;
    int total;
    scanf("%d", &days);

    for (int i = 1; i <= days; i++) {
        total = days * 400;
    }
    printf("%d\n", total);

}